// trailing commas - is comma symbol that is typed after the last item of a list of elements.

function add(param1) {
  const example = {
    name: 'Shubham',
  };
  console.log(example);
}

add();
