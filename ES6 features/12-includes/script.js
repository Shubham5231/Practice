let newNumes = [1, 2, 3, 4, 5];

console.log(newNumes.includes(1));

// includes - includes method is used to check passing value inclues is arry or object. if values inlcudes in array then it will return true otherwise false
